FOLLOW Sets
================================================================================

FOLLOW(X) = {terminals that can appear after X in any derivation}

FOLLOW(addop               ) = set()
FOLLOW(arguments           ) = {'COLON', 'SEMICOLON'}
FOLLOW(compound_stmt       ) = {'SEMICOLON', 'DOT'}
FOLLOW(declarations        ) = set()
FOLLOW(else_part           ) = {'SEMICOLON'}
FOLLOW(expr_list           ) = {'RPAREN'}
FOLLOW(expr_list_prime     ) = {'RPAREN'}
FOLLOW(expr_prime          ) = {'RBRACKET', 'COMMA', 'RPAREN'}
FOLLOW(expression          ) = {'RBRACKET', 'RPAREN', 'COMMA'}
FOLLOW(factor              ) = {'addop', 'relop', 'RBRACKET', 'COMMA', 'RPAREN', 'mulop'}
FOLLOW(factor_tail         ) = {'addop', 'relop', 'RBRACKET', 'COMMA', 'RPAREN', 'mulop'}
FOLLOW(id_list             ) = {'COLON', 'RPAREN'}
FOLLOW(id_list_prime       ) = {'COLON', 'RPAREN'}
FOLLOW(mulop               ) = set()
FOLLOW(optional_stmts      ) = set()
FOLLOW(param_list          ) = {'RPAREN'}
FOLLOW(param_list_prime    ) = {'RPAREN'}
FOLLOW(program             ) = {'EOF'}
FOLLOW(relop               ) = set()
FOLLOW(sign                ) = set()
FOLLOW(simple_expr         ) = {'RBRACKET', 'COMMA', 'RPAREN', 'relop'}
FOLLOW(simple_expr_prime   ) = {'RBRACKET', 'COMMA', 'RPAREN', 'relop'}
FOLLOW(statement           ) = {'SEMICOLON'}
FOLLOW(stmt_list           ) = set()
FOLLOW(stmt_list_prime     ) = set()
FOLLOW(subprogram_decl     ) = {'SEMICOLON'}
FOLLOW(subprogram_decls    ) = set()
FOLLOW(subprogram_head     ) = set()
FOLLOW(term                ) = {'addop', 'relop', 'RBRACKET', 'COMMA', 'RPAREN'}
FOLLOW(term_prime          ) = {'addop', 'relop', 'RBRACKET', 'COMMA', 'RPAREN'}
FOLLOW(type_spec           ) = {'SEMICOLON', 'RPAREN'}
FOLLOW(var_or_proc_call    ) = set()
FOLLOW(var_or_proc_tail    ) = set()

================================================================================
Total non-terminals: 33
